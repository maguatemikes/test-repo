/**
 * PostCSS configuration for Tailwind CSS v4 under Next.js.
 * Tailwind v4 ships its PostCSS integration via @tailwindcss/postcss.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
